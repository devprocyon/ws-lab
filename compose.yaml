services:
  api:
    build:
      dockerfile: apps/api/Dockerfile
      target: dev
    environment:
      API_PORT: 5000
    ports:
      - "5000:5000"
    volumes:
      - ./apps/api/src:/app/apps/api/src
